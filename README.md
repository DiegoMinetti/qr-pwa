# QR PWA

Generador de QR como PWA, sin backend. Guarda presets en localStorage y permite incrustar logo y fondo.

Para desarrollo local:

```bash
# servir desde un servidor estático (por ejemplo http-server)
npm install -g http-server
http-server . -p 8080
```

Se incluye workflow para desplegar a GitHub Pages automáticamente.
